{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the island window",
  "windows": ["island"],
  "permissions": ["core:default"]
}
