# tech.md — ядро проекта Peekle

**Версия ядра: v2 (2026-08-17)**

Changelog:
- v2 — горячая клавиша переведена с двойного нажатия `q` на комбинацию через Carbon RegisterEventHotKey (разрешение Accessibility больше не нужно). Usage переведён на аккаунт пользователя: OAuth-токен из Keychain плюс rate-limit заголовки Anthropic, окна стали 5h и 7d вместо day и week. Добавлена секция 17 с решениями, взятыми из claudecodenotify. Подписание отложено: v1 выходит ad-hoc подписью через Homebrew.
- v1 — первичная заморозка: стек, привязка хуков, HTTP-контракт, типы, команды и события Tauri, контракт окон, схема конфига, провайдеры, UI-примитивы, доктрина тестов, роадмап.

Этот файл единственный источник истины. Каждая сессия подчиняется ему дословно. Контракты не выдумываются. Нет нужного типа, поля, эндпоинта или события: СТОП и блок `CONTRACT GAP` (секция 13). Изменения контрактов только append-only и бампают версию в шапке.

Язык репозитория английский: код, комментарии, коммиты, PR, UI-копия. Этот файл и CLAUDE.md на русском, потому что их читаешь ты и сессия, а не пользователь продукта.

---

## 1. Проект

Peekle это оверлей поверх Claude Code для macOS. Разработчик запускает агента и уходит: смотрит кино, читает, работает в другом фуллскрин-окне. Агент закончил турн или запросил разрешение: Peekle поднимает окно в стиле Spotlight поверх всего, показывает последнее сообщение агента, принимает текст или выбор из списка и исчезает. Ответ уходит прямо в турн агента, работа продолжается без переключения в терминал.

Плюс полупрозрачный HUD со списком задач в левом верхнем углу и тост в стиле Dynamic Island по центру сверху для смены состояний.

Целевой пользователь: соло-разработчик с одной или несколькими сессиями Claude Code на macOS. Распространение: Homebrew и npm.

Тест успеха продукта: пользователь отрабатывает сорокаминутную сессию агента, отвечает на четыре запроса и ни разу не фокусирует терминал и не сворачивает видео.

Ключевое отличие от claudecodenotify (секция 17): тот только уведомляет и отправляет тебя в терминал, решение принимается там. Peekle принимает решение в самом оверлее и возвращает его агенту. Это меняет всё: хуки блокирующие, а не fire-and-forget.

## 2. Границы

В v1 входит:
- macOS 13+, Apple Silicon и Intel.
- Сессии Claude Code, запущенные из любого терминала на этой же машине.
- Окно запроса, HUD задач, island-тост, полосы usage, глобальный тумблер, CLI-установщик, упаковка.

В v1 не входит, не строим:
- Оконный код для Windows и Linux. Весь AppKit держим за `#[cfg(target_os = "macos")]` и за трейтом, чтобы порт был дополнением, а не переписыванием.
- Удалённые и контейнерные сессии Claude Code.
- Правка конфигов, промптов и прав агента шире, чем решение allow или deny, которое возвращает панель.
- Аккаунты, телеметрия, облачная синхронизация, GUI настроек. Конфиг это файл.
- Нотаризация у Apple. Едем на ad-hoc подписи, см. секцию 17 и R-7.

Продуктовые решения, а не пробелы: у Peekle нет кнопки закрытия, нет иконки в трее и в доке, и ни одно окно не остаётся на экране после ответа.

## 3. Стек (заморожен)

Фронтенд:
- SvelteKit 2, `@sveltejs/adapter-static`, `ssr = false`, `prerender = true`.
- Svelte 5, только runes (`$state`, `$derived`, `$effect`). Никаких `svelte/store` в новом коде.
- TypeScript strict.
- Tailwind CSS v4, дизайн-токены как CSS custom properties (секция 9).
- `bits-ui` для headless-листбокса и клавиатурной навигации. Стилизация наша. Полный UI-кит не тянем.
- `@tauri-apps/api` v2. Все вызовы идут через `src/lib/bridge` (секция 5).

Бэкенд:
- Rust stable, пин в `rust-toolchain.toml`, edition 2021.
- Tauri v2 (2.10 или новее).
- `tauri-nspanel` (ветка `v2.1`) для конвертации окон в NSPanel.
- `tauri-plugin-global-shortcut` для комбинации клавиш.
- `tauri-plugin-autostart` (Open at Login), `tauri-plugin-updater` (проверка обновлений).
- `axum` + `tokio` для локального сервера хуков.
- `reqwest` (rustls) для запроса usage.
- `serde`, `serde_json`, `ulid`, `thiserror`, `tracing`, `directories`, `toml`.
- `ts-rs` для генерации типов во фронтенд.
- `objc2` для флагов панели, которых нет в `tauri-nspanel`.

Тулинг:
- pnpm, Node 20+.
- Тесты: `cargo test`, `vitest` + `@testing-library/svelte`, Playwright по роутам, `fast-check` (чистая логика TS), `proptest` (чистая логика Rust).
- Линт: `cargo fmt`, `cargo clippy -D warnings`, `eslint`, `prettier`, `svelte-check`.
- CI: GitHub Actions на `macos-14`.
- DMG: `dmgbuild`, сборка образа без автоматизации Finder.

Базы данных нет. Состояние живёт в памяти Rust. На диск ложатся только конфиг, токен и port-файл.

## 4. Архитектура

Claude Code управляет Peekle через хуки. Peekle держит HTTP-сервер на loopback, хуки прописаны как `type: "http"` и бьют в него. Запрос хука это событие, тело HTTP-ответа это решение, которое Claude Code исполняет.

```
турн Claude Code
  |
  |  POST /v1/h/<token>/<endpoint>   (JSON хука в теле)
  v
peekle-server (axum, 127.0.0.1)
  |  регистрирует pending-запрос, эмитит событие Tauri
  v
состояние в Rust  ->  панель запроса / HUD / island  (роуты SvelteKit)
  |                                     |
  |  <- invoke answer_prompt(...)  <-----
  v
тело HTTP-ответа  ->  Claude Code продолжает турн
```

Почему HTTP-хуки, а не command-хук с шелл-скриптом: нет спавна процесса на каждое событие, сам запрос и есть ожидание ответа, а если приложение не запущено, соединение падает, Claude Code считает это non-blocking ошибкой и работает обычным образом. Деградация бесплатная, агент никогда не висит на мёртвом оверлее.

Три окна, три роута, один владелец состояния (Rust). Фронтенд не хранит авторитетного состояния: он рендерит `PeekleState` и отправляет интенты.

Модель блокировки: эндпоинты `stop` и `permission` кладут запрос в pending-реестр по id и ждут `oneshot`. Канал разрешается на ответе, отмене, таймауте или байпасе. Разрешается каждый путь: утёкший pending-запрос вешает агента до таймаута хука.

## 5. Раскладка репозитория

```
peekle/
  CLAUDE.md
  tech.md
  Cargo.toml                     # workspace: src-tauri + crates/*
  rust-toolchain.toml
  package.json
  svelte.config.js
  vite.config.ts
  src/
    routes/
      +layout.svelte             # токены, прозрачный body, без скроллбаров
      prompt/+page.svelte
      hud/+page.svelte
      island/+page.svelte
      kitchen-sink/+page.svelte  # все примитивы, только dev
    lib/
      ui/                        # примитивы, секция 9
      types/generated/           # вывод ts-rs, руками не правится
      bridge/                    # обёртки invoke и событий, единственное место с @tauri-apps/api
      features/
        prompt/
        hud/
        island/
        usage/
      logic/                     # чистый TS, покрыт property-тестами
  src-tauri/
    src/{main.rs,lib.rs,windows.rs,panel.rs,commands.rs,events.rs,state.rs}
    tauri.conf.json
  crates/
    peekle-core/                 # типы, pending-реестр, чистая логика, экспорт ts-rs
    peekle-server/               # эндпоинты axum, маппинг хуков
    peekle-usage/                # Keychain, запрос usage, провайдеры
    peekle-hotkey/               # регистрация комбинации, разбор строки
    peekle-cli/                  # peekle init | uninstall | doctor | status | off
  fixtures/hooks/                # захваченные payload-ы хуков, golden-файлы
  packaging/homebrew/peekle.rb
  npm/                           # npm-обёртка
  tests/e2e/                     # Playwright
  .github/workflows/{ci.yml,release.yml}
```

Правило: фича владеет одной папкой в `src/lib/features/<name>` плюс одним роутом. Общий код идёт в `src/lib/ui`, `src/lib/logic` или в крейт, а это уже изменение контракта.

## 6. Контракты (заморожены, v2)

### 6.1 Привязка хуков

`peekle init` пишет эти хендлеры в `~/.claude/settings.json`, мёржит с существующими и никогда не удаляет чужие записи. `PORT` и `TOKEN` берутся из конфига (6.8). Запись идемпотентна: повторный запуск заменяет только хендлеры, у которых host равен `127.0.0.1:PORT`, а путь начинается на `/v1/h/`. Перед каждой записью делается бэкап `settings.json.peekle-bak.<unix-ts>`.

| Событие Claude Code | matcher | эндпоинт | блокирует | зачем |
|---|---|---|---|---|
| `Stop` | нет | `/v1/h/<token>/stop` | да | агент закончил турн, спрашиваем что дальше |
| `PermissionRequest` | `*` | `/v1/h/<token>/permission` | да | инструменту нужно allow или deny |
| `Notification` | `permission_prompt\|idle_prompt\|agent_needs_input\|agent_completed` | `/v1/h/<token>/notification` | нет | island-тост, живость сессии |
| `PostToolUse` | `TodoWrite` | `/v1/h/<token>/tasks` | нет | источник истины для HUD задач |
| `TaskCreated`, `TaskCompleted` | нет | `/v1/h/<token>/tasks` | нет | вторичный сигнал по задачам, см. R-4 |
| `SessionStart`, `SessionEnd` | нет | `/v1/h/<token>/session` | нет | реестр сессий, группировка HUD |

Форма хендлера, которую пишет `init`:

```json
{
  "type": "http",
  "url": "http://127.0.0.1:47821/v1/h/<token>/stop",
  "timeout": 900
}
```

`timeout` важен: у таймаутнувшего хука вывод отбрасывается, решения нет, и агент останавливается обычным образом. Peekle обязан ответить раньше, поэтому `behavior.prompt_timeout_secs` держится ниже `timeout` хука (600 против 900).

Токен идёт сегментом пути, а не заголовком: интерполяция env в заголовки требует списка `allowedEnvVars` и добавляет подвижную деталь. URL лежит в локальном файле пользователя, сервер слушает только loopback и отбивает любой другой путь.

Список управляемых событий это одна константа `peekle-core::MANAGED_HOOK_EVENTS`. Она же используется в `uninstall` и в `doctor`.

### 6.2 HTTP-эндпоинты

Тела запросов это сырой JSON хука Claude Code. Неизвестные поля игнорируются, а не отбиваются: Claude Code добавляет поля между версиями.

| Метод | Путь | Ответ при ответе пользователя | Ответ при отмене, таймауте или выключенном Peekle |
|---|---|---|---|
| POST | `/v1/h/{token}/stop` | `{"decision":"block","reason":"<текст пользователя>"}` | `200 {}` |
| POST | `/v1/h/{token}/permission` | см. ниже | `200 {}` |
| POST | `/v1/h/{token}/notification` | `200 {}` | `200 {}` |
| POST | `/v1/h/{token}/tasks` | `200 {}` | `200 {}` |
| POST | `/v1/h/{token}/session` | `200 {}` | `200 {}` |
| GET | `/v1/health` | `200 {"version":"...","enabled":bool,"core":"v2"}` | то же |

Неверный токен: `404`, без тела, значение токена в логи не попадает. Тело не JSON: `400`. Любая внутренняя ошибка: `200 {}`, потому что 500 для Claude Code это non-blocking ошибка, а пустое решение всё равно безопасный фолбэк. На путях хуков не возвращаем ничего вне 2xx, кроме `404` и `400` выше.

Маппинг ответа `stop`:
- свободный текст: `{"decision":"block","reason":"<текст>"}`. Reason это следующий промпт, по которому агент действует, поэтому текст передаётся дословно, только с обрезкой пробелов, без обёрточной прозы.
- выбор `finish`: `200 {}`, турн заканчивается штатно.
- выбор `continue`: `{"decision":"block","reason":"Continue with the current plan."}`.

Маппинг ответа `permission`:

```json
{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}
{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"deny"}}}
```

При deny в reason уходит текст пользователя, если он его набрал. `allow_always` возвращает `allow` и запоминает правило локально на сессию: правила прав в настройки Claude Code Peekle в v1 не пишет.

Семантика exit-кодов к HTTP-хукам не применяется. Решение через статус-код не выражаем никогда.

### 6.3 Типы ядра

Источник истины это Rust в `crates/peekle-core/src/types.rs`. `ts-rs` экспортирует в `src/lib/types/generated/`. Правка сгенерированного TS руками это падение CI.

```rust
pub struct SessionRef { pub session_id: String, pub cwd: String, pub project: String }

pub enum PromptKind { Stop, Permission, Idle }

pub struct PromptRequest {
    pub id: String,                    // ulid, выдаёт peekle
    pub kind: PromptKind,
    pub session: SessionRef,
    pub title: String,                 // одна строка, например "Claude finished"
    pub last_message: Option<String>,  // last_assistant_message, обрезка до 2000 символов
    pub detail: Option<String>,        // имя инструмента и превью входа, 400 символов
    pub options: Vec<ChoiceOption>,    // пусто значит только свободный текст
    pub allow_free_text: bool,
    pub created_at: i64,               // unix ms
    pub expires_at: i64,
}

pub struct ChoiceOption { pub id: String, pub label: String, pub hint: Option<String>, pub kind: ChoiceKind }
pub enum ChoiceKind { AllowOnce, AllowAlways, Deny, Continue, Finish, Custom }

pub struct PromptAnswer { pub prompt_id: String, pub choice: Option<String>, pub text: Option<String> }
pub enum PromptOutcome { Answered(PromptAnswer), Dismissed, TimedOut, Bypassed }

pub struct TaskItem {
    pub id: String, pub title: String, pub label: TaskLabel,
    pub status: TaskStatus, pub session_id: String, pub updated_at: i64,
}
pub enum TaskLabel { Code, Fix, Bug, Test, Docs, Chore, Research }
pub enum TaskStatus { Pending, Active, Done }

pub enum UsageWindow { FiveHour, SevenDay }
pub struct UsageWindowStat {
    pub window: UsageWindow,
    pub used_pct: f32,            // 0.0 .. 100.0
    pub resets_at: Option<i64>,   // unix seconds
}
pub enum UsageSource { Account, Fake, Unavailable }
pub enum UsageUnavailable { Disabled, NotGranted, Denied, NotLoggedIn, Network, Unsupported }
pub struct UsageSnapshot {
    pub windows: Vec<UsageWindowStat>,
    pub source: UsageSource,
    pub reason: Option<UsageUnavailable>,
    pub fetched_at: i64,
}

pub enum ToastTone { Neutral, On, Off, Warn }
pub struct ToastRequest { pub text: String, pub tone: ToastTone, pub ttl_ms: u32, pub badge: Option<u32> }

pub struct PeekleState {
    pub enabled: bool,
    pub active_prompt: Option<PromptRequest>,
    pub tasks: Vec<TaskItem>,          // свежая активность первой, кап 50
    pub usage: UsageSnapshot,
    pub live_sessions: u32,
    pub hotkey_ok: bool,               // false если комбинация занята другим приложением
}
```

`used_pct` клампится в 0..100. `TaskLabel` выводит классификатор `peekle-core::labels` из заголовка задачи, он чистый и покрыт property-тестами, неизвестное падает в `Code`.

Активный запрос всегда один. Второй блокирующий хук, пришедший при `active_prompt = Some`, встаёт в FIFO-очередь за ним. Очередь не часть `PeekleState`, фронтенд её не видит.

### 6.4 Usage через аккаунт пользователя

Официального API квот нет, но у пользователя уже есть залогиненный Claude Code, и Anthropic отдаёт утилизацию лимитов в заголовках ответа на любой запрос с его OAuth-токеном. Механика проверена в claudecodenotify (секция 17), берём её целиком. Никакого API-ключа и никакого отдельного биллинга: работаем от аккаунта пользователя, нужно только его разрешение на чтение Keychain.

Шаг 1, токен. Читаем Keychain через дочерний процесс `/usr/bin/security`, а не через `SecItemCopyMatching` в своём процессе. Причина: ACL Keychain привязывается к запрашивающему бинарю, а `security` это стабильно подписанный инструмент Apple, поэтому выданный однажды Always Allow держится между перезапусками и обновлениями приложения. У нашего ad-hoc подписанного бинаря разрешение спрашивалось бы каждый запуск.

```
/usr/bin/security find-generic-password -s "Claude Code-credentials" -a <NSUserName> -w
```

Коды выхода: `0` успех (stdout это JSON, с конца снять `\n`), `128` пользователь нажал Deny, `44` записи нет (пользователь не логинился через CLI), остальное транзиентная ошибка. Токен лежит в `claudeAiOauth.accessToken`.

Разрешение спрашивается только по действию пользователя: кнопка в онбординге или команда `request_usage_access`. На старте приложения диалог Keychain не поднимаем никогда. `Denied` запоминается в конфиге и больше не спрашивается до явного сброса.

Шаг 2, запрос. Минимальный запрос, нужны только заголовки ответа:

```
POST https://api.anthropic.com/v1/messages
anthropic-version: 2023-06-01
anthropic-beta: oauth-2025-04-20
content-type: application/json
user-agent: claude-code/<version>
authorization: Bearer <accessToken>

{"model":"claude-haiku-4-5-20251001","max_tokens":1,"messages":[{"role":"user","content":"hi"}]}
```

Таймаут 10 секунд. Тело ответа игнорируется целиком.

Шаг 3, заголовки:

| Заголовок | Смысл |
|---|---|
| `anthropic-ratelimit-unified-5h-utilization` | доля использования пятичасового окна |
| `anthropic-ratelimit-unified-5h-reset` | unix seconds, когда окно сбросится |
| `anthropic-ratelimit-unified-7d-utilization` | доля использования недельного окна |
| `anthropic-ratelimit-unified-7d-reset` | unix seconds |

Утилизация парсится как доля 0..1 и умножается на 100 в `used_pct`. Проверить на первом живом ответе: если заголовок отдаёт 0..100, поправить здесь и бампнуть версию ядра. Нет хотя бы одного из четырёх заголовков: `Unavailable(Unsupported)`, числа не выдумываем.

Окна именно 5h и 7d, а не день и неделя: это то, что реально отдаёт бэкенд. В UI подписи `5h` и `Week`.

Шаг 4, `401` или `403`: токен просрочен. Один раз за пять минут пробуем дать CLI обновить запись в Keychain, затем перечитываем токен и повторяем запрос ровно один раз.

```
<claude> -p hi --max-budget-usd 0.01
```

Путь к `claude` ищем по кандидатам в порядке: `$HOME/.local/bin/claude`, `/opt/homebrew/bin/claude`, `/usr/local/bin/claude`. Не нашли: `Unavailable(NotLoggedIn)`.

Кадентность: фоновый опрос раз в 5 минут, плюс перед показом панели, если снапшот старше 60 секунд. Опрос никогда не задерживает `prompt-open`: панель открывается на прошлом снапшоте, свежий приезжает событием.

Честность копии в UI: рядом с процентом обратный отсчёт до сброса, слова limit и quota не используем. Числа снимаются с недокументированных заголовков и могут поменяться.

Стоимость: один запрос это один токен на haiku раз в 5 минут. Заметно в счётчиках, не в кошельке. `[usage] enabled = false` выключает опрос полностью.

### 6.5 Команды Tauri

Точные имена и payload-ы. Фронтенд не вызывает ничего другого.

| Команда | Аргументы | Возврат | Заметки |
|---|---|---|---|
| `get_state` | нет | `PeekleState` | один раз на маунт окна |
| `answer_prompt` | `PromptAnswer` | `()` | разрешает pending-хук, прячет панель |
| `dismiss_prompt` | `{ prompt_id: String }` | `()` | разрешает как `Dismissed` |
| `set_enabled` | `{ enabled: bool }` | `()` | режим байпаса, эмитит тост |
| `refresh_usage` | нет | `UsageSnapshot` | ручное обновление, панель не блокирует |
| `request_usage_access` | нет | `UsageSnapshot` | только по действию пользователя, поднимает диалог Keychain |
| `set_usage_enabled` | `{ enabled: bool }` | `()` | пишет конфиг, гасит или поднимает опрос |
| `dev_emit_prompt` | `PromptRequest` | `()` | только debug, `#[cfg(debug_assertions)]` |

Ответ на уже разрешённый prompt id это no-op, а не ошибка. Тест на это в секции 10.

### 6.6 События Tauri

Rust во фронтенд. Имена буквальные.

| Событие | Payload | Куда |
|---|---|---|
| `peekle://prompt-open` | `PromptRequest` | prompt |
| `peekle://prompt-close` | `{ prompt_id: String, outcome: PromptOutcome }` | prompt |
| `peekle://tasks` | `Vec<TaskItem>` | hud |
| `peekle://usage` | `UsageSnapshot` | prompt |
| `peekle://enabled` | `{ enabled: bool }` | все |
| `peekle://toast` | `ToastRequest` | island |

Фронтенд не эмитит события Tauri. Интенты только через команды.

### 6.7 Контракт окон

| Label | Роут | Размер и позиция | Флаги панели |
|---|---|---|---|
| `prompt` | `/prompt` | ширина 720, высота авто до 520, по центру по горизонтали, верх на 22% высоты экрана | nonactivating panel, становится key чтобы работал ввод, уровень `screen_saver`, `join_all_spaces` + `full_screen_auxiliary`, `hides_on_deactivate(false)`, без светофора |
| `hud` | `/hud` | ширина 380, высота авто до 344, отступ слева сверху 24 x 52 | никогда не становится key, принимает мышь и скролл, уровень `floating`, то же collection behavior |
| `island` | `/island` | 340 x 72, по центру сверху, отступ сверху 8 | никогда не становится key, полностью игнорирует мышь, уровень `screen_saver` |

Все три объявлены в `tauri.conf.json` как `visible: false`, `decorations: false`, `transparent: true`, `skipTaskbar: true` и конвертируются в панели до первого показа. `macOSPrivateApi: true`, activation policy `Accessory`: нет иконки в доке, нет строки меню.

Ловушки, которые являются контрактом, а не вкусом:
- `hides_on_deactivate(false)` на каждой панели. Дефолт прячет панель при деактивации приложения, а для неактивирующегося оверлея приложение деактивировано всегда.
- `full_screen_auxiliary()` на каждой панели, иначе поверх фуллскрин-видео не появится ничего и ошибки не будет.
- `canBecomeKeyWindow` false для `hud` и `island`. HUD, съевший нажатие в терминале, убивает продукт.
- Показ и скрытие, а не создание и уничтожение. Конвертация в панель делается один раз на старте.

Escape прячет панель запроса и разрешает pending-хук как `Dismissed`. Другого способа закрыть окно из UI нет.

### 6.8 Конфиг

`~/Library/Application Support/peekle/config.toml` через `directories`, права `0600`. Пишется `peekle init`, читается на старте, отслеживается на изменения.

```toml
[server]
port = 47821
token = "<32 hex, генерится один раз>"

[hotkey]
toggle = "Alt+Shift+KeyQ"      # комбинация в формате tauri-plugin-global-shortcut
recall = ""                    # пусто = не регистрировать

[ui]
prompt_opacity = 0.92
hud_opacity = 0.55
blur = true
hud_visible_tasks = 6

[usage]
enabled = true
provider = "account"           # account | fake | off
keychain_denied = false        # выставляет приложение, сбрасывает пользователь

[behavior]
enabled = true
prompt_timeout_secs = 600
```

Неизвестные ключи игнорируются с варнингом. Нет файла: дефолты плюс свежий токен. `hud_visible_tasks` больше 6 клампится до 6. Порт дублируется в `~/.peekle/port` для `doctor` и `status`.

### 6.9 Горячая клавиша

Регистрация через `tauri-plugin-global-shortcut`, под капотом Carbon `RegisterEventHotKey`. Это даёт три вещи, которых не было у варианта с двойным нажатием `q`:
- разрешение Accessibility не нужно вообще;
- комбинация перехватывается системно и не доходит до приложения под фокусом, то есть `q` в плеере больше не закрывает плеер;
- ложных срабатываний при наборе текста не бывает.

Дефолт: `Alt+Shift+KeyQ` (⌥⇧Q). Мнемоника Q от quiet, `⌘Q` не задействован, так что ничего важного не отбираем.

Действие: тумблер `enabled`. `OFF` значит, что каждый блокирующий эндпоинт немедленно отвечает `200 {}` и агент работает в своём обычном режиме, а задачи и сессии продолжают собираться. Island показывает `Peekle is ON` или `Peekle is OFF` на 1600 мс.

Регистрация может провалиться, если комбинацию уже держит другое приложение. Тогда: `hotkey_ok = false`, один island-варнинг на старте, запись в `doctor`, всё остальное работает. Молча глотать провал нельзя.

Строка комбинации разбирается функцией `peekle-hotkey::parse`, чистой и покрытой property-тестами на round-trip разбора и отображения (⌥⇧Q для онбординга и `doctor`).

## 7. Провайдеры и фейки

Каждая внешняя зависимость сидит за трейтом с фейком, и в dev по умолчанию подключён фейк. Разработка не ждёт ни реальных данных, ни реальных разрешений.

| Трейт | Реальная реализация | Фейк |
|---|---|---|
| `UsageProvider` | `AccountUsage`: Keychain плюс rate-limit заголовки по секции 6.4 | `FakeUsage`: фиксированные проценты плюс режим, отдающий каждый вариант `UsageUnavailable` |
| `CredentialStore` | `SecurityToolStore`: `/usr/bin/security`, коды 0, 128, 44 | подставной стор в temp-каталоге |
| `ClaudeSettings` | чтение и мёрж `~/.claude/settings.json` | temp-каталог, используется в каждом тесте `peekle init` |
| `HotkeyRegistrar` | `tauri-plugin-global-shortcut` | фейк, дёргается тестовой командой, умеет отдавать провал регистрации |
| `TaskFeed` | эндпоинты хуков | проигрывает `fixtures/hooks/*.jsonl` по порядку |

`UsageProvider`, отдающий `Unavailable`, это нормальное состояние, а не ошибка: полосы рендерятся прочерками с понятной подписью причины, панель работает. Usage никогда не лежит на критическом пути ответа на запрос.

## 8. Правила потоков данных

- Состоянием владеет Rust. Фронтенд рендерит и отправляет интенты.
- Каждый блокирующий хук разрешается ровно один раз. `oneshot`, sender дропается на разрешении.
- `enabled == false` замыкает до любой оконной работы: сразу `200 {}`, сбор задач и сессий продолжается, HUD живёт.
- Порядок показа панели: зарегистрировать pending, эмитнуть `peekle://prompt-open`, дождаться `ready` от вебвью до 250 мс, показать панель. Показ до рендера контента даёт видимую пустую коробку.
- Скрытие панели делает Rust после разрешения команды, а не вебвью.
- Диалог Keychain поднимается только из `request_usage_access`.

## 9. Дизайн-токены и UI-примитивы

Визуальное ограничение: оверлей лежит поверх кино. Никаких больших непрозрачных поверхностей, никакого затемнения всего экрана, никакой чисто белой заливки больше 40% площади панели.

Токены в `src/routes/+layout.svelte` как CSS custom properties:

```
--surface: rgba(12,12,14,0.72)      /* панель запроса */
--surface-hud: rgba(12,12,14,0.48)  /* hud, island */
--hairline: rgba(255,255,255,0.08)
--text: rgba(255,255,255,0.94)
--text-dim: rgba(255,255,255,0.56)
--accent: #7dd88f
--warn: #e8b34a
--danger: #e8654a
--blur: 24px
--radius: 14px
--row: 34px
```

Подложка: `backdrop-filter: blur(var(--blur)) saturate(140%)`. Фон body во всех роутах остаётся `transparent`.

Примитивы собираются один раз в `src/lib/ui` и используются всеми фичами. Ни одна фича не пишет свою кнопку, строку или полосу.

| Примитив | Набросок пропсов |
|---|---|
| `Panel` | `{ tone?: 'prompt' \| 'hud' \| 'island', padded?: boolean }`, слот |
| `PromptInput` | `{ value, placeholder, disabled, onsubmit, onescape }`, автофокус, Enter отправляет, Shift+Enter перевод строки |
| `OptionList` | `{ options: ChoiceOption[], selected, onselect }`, листбокс bits-ui, стрелки и цифры |
| `MessageBlock` | `{ text: string, collapsed: boolean }`, кламп в 6 строк с раскрытием |
| `UsageBar` | `{ label: string, pct: number \| null, resetsAt?: number, reason?: string }`, прочерки при null, цвет по порогам 50, 75, 90 (accent, warn, orange, danger) |
| `TaskRow` | `{ task: TaskItem }`, точка статуса, заголовок с эллипсисом, пилюля метки |
| `LabelPill` | `{ label: TaskLabel }`, свой цвет на метку, капс, 10px |
| `ScrollHint` | `{ visible: boolean }`, шеврон вниз, гаснет во время скролла |
| `Toast` | `{ text, tone, badge? }`, island-пилюля, spring на входе и выходе |
| `Kbd` | `{ keys: string[] }` |

Роут `kitchen-sink` рендерит каждый примитив во всех состояниях и служит целью визуальной регрессии.

## 10. Доктрина тестов

Тесты привязаны к слайсу и его PR, не к стадии. Слайс мёржится только с тестами, гейт красный без них.

Тесты выводятся из критериев приёмки задачи, никогда из реализации. Ловушка, которую избегаем: сначала пишется код, потом тесты, подтверждающие, что код делает то, что делает, вместе с багами. Тест кодирует контракт.

Обязательно на слайс:

- **Контрактные тесты хуков.** Golden-payload-ы из `fixtures/hooks/` шлются POST-ом в реальный роутер axum в процессе. Ответ сверяется с формой из 6.2 поле за полем. Каждый payload захвачен с живой сессии, ни один не написан руками. Это самые ценные тесты проекта: больше контракт Claude Code проверять негде.
- **Тесты единственного разрешения, аналог идемпотентности.** Один и тот же payload дважды: один pending, один показ окна. Ответ на один prompt id дважды: один HTTP-ответ, второй вызов no-op. Ответ после таймаута: без паники и без второго ответа. Незакрытый pending вешает агента, поэтому тест на каждый путь.
- **Путь ошибки.** Keychain отдал `128` и `44`, `/v1/messages` отдал 401 с последующим провалом refresh, нет одного из rate-limit заголовков, порт занят, комбинация клавиш занята, битое тело хука. В каждом случае панель открывается и отвечает, а причина видна в `UsageUnavailable` или в `doctor`.
- **Property-based на чистой логике.** `proptest` в Rust и `fast-check` в TS: математика окон usage (любой набор заголовков даёт 0..100, кламп, монотонность), классификатор меток (тотальная функция, не паникует, стабильна), окно HUD (при любом числе задач рендерится максимум 6, `ScrollHint` виден ровно когда задач больше 6), разбор строки комбинации (round-trip parse и display).
- **Компонентные тесты** на vitest и `@testing-library/svelte` по клавиатуре: Enter отправляет, Shift+Enter переводит строку, стрелки двигают выбор, Escape отменяет.
- **Playwright** против `vite dev` по трём роутам с замоканным bridge. Playwright не гоняет нативную панель, её поведение проверяется руками по чек-листу секции 15.

## 11. Владение инфраструктурой

- `~/.claude/settings.json` это территория пользователя. Пишет только `peekle-cli`, только через `ClaudeSettings`, всегда мёржем, всегда идемпотентно, всегда с бэкапом с таймстемпом перед записью. `peekle uninstall` снимает только свои записи и убирает ключ `hooks`, если он опустел.
- Токен сервера и credentials никогда не попадают в логи и в трейсы. В `tracing` идёт длина токена, не значение.
- Конфиг и дефолты живут в `peekle-core::config`. Никто не читает env-переменные вразброс.
- Фикстуры в `fixtures/hooks/` общие для тестов и для фейкового фида. Новая фикстура добавляется скриптом `scripts/capture-hooks.sh`, а не руками.
- Сгенерированный TS в `src/lib/types/generated/` производит `cargo test -p peekle-core export_bindings`. CI гоняет его и падает на грязном дереве.
- Артефакты релиза и формулу Homebrew производит только `release.yml` по тегу.

Гейты CI:
- Гейт на PR: `cargo fmt --check`, `cargo clippy -D warnings`, `cargo test`, проверка дрейфа ts-rs, `pnpm lint`, `svelte-check`, `pnpm test`, `pnpm test:e2e`, `pnpm tauri build --debug`. Без деплоя.
- Тег `v*`: сборка, ad-hoc подпись, dmg через `dmgbuild`, аплоад в релиз, обновление формулы каска.

Branch protection на `main`: только через PR, мёрдж на зелёном.

## 12. Конвенции кода

- Язык всего в репозитории английский: коммиты, заголовки и тела PR, комментарии в коде, UI-копия. Исключение: этот файл и CLAUDE.md.
- Формат коммита фиксированный, всегда Conventional Commits: `type(scope): summary`. `type` из набора `feat|fix|test|refactor|chore|docs`. Summary в императиве, со строчной, без точки, до ~50 символов. Тело только про почему, не про что.
- Сессия коммитит по ходу работы маленькими логическими коммитами после каждого осмысленного шага, а не сваливает всё в конце. Каждый коммит по возможности проходит тайпчек.
- Scope-ы: `prompt`, `hud`, `island`, `usage`, `hotkey`, `server`, `cli`, `ui`, `core`, `pkg`, `ci`.
- Заголовок PR несёт id слайса из секции 14, например `S4 usage bars`. Тело: что делает слайс, какие контракты затрагивает, чем покрыт тестами. Коротко.
- Комментарии объясняют почему. Комментарий не пересказывает строку под собой. Закомментированный код в PR не остаётся.
- Дисциплина прозы в английских артефактах: активный залог, императив, без филлеров, без em-dash, без эмодзи.
- Rust: `thiserror` в библиотеках, `anyhow` только в бинарях. Никаких `unwrap` вне тестов и никогда в хендлере хука. AppKit изолирован в `panel.rs`.
- Svelte: только runes, без `on:`-директив, пропсы через `$props()`, один компонент на файл, в разметке логика не сложнее тернарника.

## 13. Протокол CONTRACT GAP

Когда задаче нужен тип, поле, эндпоинт, событие, команда или ключ конфига, которого в этом файле нет:

1. СТОП. Не выдумывать и не писать код против выдуманного типа.
2. Выдать блок ровно в такой форме:

```
CONTRACT GAP
Need: что не хватает
Why: какая задача и какой критерий приёмки этого требует
Proposed shape: точный тип Rust, эндпоинт или подпись события
Blast radius: какие слайсы и файлы меняются
Local workaround: заглушка, на которой едем дальше
```

3. Продолжать на локальной заглушке внутри папки слайса.
4. Контракт аппендится в этот файл с бампом версии до мёрджа слайса. PR, который меняет контракт и код фичи одним коммитом, отклоняется: сначала ложится контракт.

То же правило на расхождение: фикстура показала, что Claude Code присылает не то, что написано в секции 6. Фикстура права, файл правится, версия бампается. Точно так же с заголовками usage из 6.4.

## 14. Роадмап

### Стадия 1: каркас

По пункту на PR это нормально, но ни один фичевый слайс не начинается, пока весь чек-лист не зелёный.

1. Репозиторий, воркспейсы pnpm и Cargo, `rust-toolchain.toml`, конфиги prettier и eslint, `.gitignore`.
2. Workflow CI с полным гейтом PR, зелёный на тривиальном PR.
3. Статическая сборка SvelteKit внутри Tauri v2, три роута рендерятся, activation policy `Accessory`, иконки в доке нет.
4. `panel.rs`: все три окна конвертированы в NSPanel с флагами из 6.7, команды показа и скрытия, Escape подключён.
5. Дизайн-токены и все примитивы секции 9, отрендеренные в `kitchen-sink`.
6. `peekle-core`: типы из 6.3, pending-реестр, модуль конфига, классификатор меток, экспорт `ts-rs` в CI.
7. `peekle-server`: роутер axum со всеми эндпоинтами 6.2, проверка токена, терпимость к неизвестным полям, health.
8. Фейки всех пяти провайдеров секции 7, выбираются конфигом, в dev по умолчанию.
9. Скрипт захвата фикстур и первые захваченные payload-ы для `Stop`, `PermissionRequest`, `Notification`, `PostToolUse/TodoWrite`.
10. Эталонная вертикаль от начала до конца: `POST /notification` в `ToastRequest` в `peekle://toast` в island-панель и авто-скрытие. Маленькая нарочно. Каждый следующий слайс копирует её раскладку файлов, использование bridge, раскладку тестов и нейминг.
11. Харнесс всех шести типов тестов с одним живым примером на каждый.
12. `release.yml`, отдающий ad-hoc подписанный dmg как артефакт.

Чек-лист готовности каркаса, всё или ничего:
- [ ] CI зелёный на тривиальном PR
- [ ] все три панели показываются поверх фуллскрин-видео и не забирают фокус
- [ ] каждый примитив рендерится в `kitchen-sink`
- [ ] `curl` в каждый эндпоинт возвращает документированную форму
- [ ] `cargo test -p peekle-core export_bindings` оставляет дерево чистым
- [ ] фейки отдают данные фикстур при `provider = "fake"`
- [ ] эталонная вертикаль работает от живой сессии Claude Code
- [ ] dmg устанавливается и запускается на чистой учётке после `xattr -dr com.apple.quarantine`

### Стадия 2: слайсы фич

Один слайс, один PR, тесты внутри. Порядок это порядок зависимостей.

**S1 Панель запроса, путь Stop.** Владеет `src/lib/features/prompt`, `peekle-server::stop`. Хук `Stop` открывает панель с заголовком и полем ввода, Enter отвечает, хук возвращает `decision: block` с текстом, панель прячется за 120 мс после ответа. Escape отменяет, турн заканчивается штатно. Приёмка: живая сессия продолжается по набранной инструкции без касания терминала. Тесты: контракт на захваченной фикстуре `Stop`, пара на единственное разрешение, компонентный на Enter и Shift+Enter, путь ошибки на отмену после таймаута.

**S2 Список выбора, путь permission.** `PermissionRequest` открывает панель с `options`: allow once, allow for this session, deny, плюс опциональный текст как причина deny. Стрелки и цифры 1..4 выбирают, Enter подтверждает. Приёмка: запрос прав на `Bash` закрывается из панели и агент едет дальше. Тесты: контракт на каждое behavior, таблица маппинга 6.2 прогнана на каждом `ChoiceKind`, клавиатура листбокса.

**S3 Последнее сообщение агента.** `MessageBlock` в панели, источник `last_assistant_message`, кламп в 6 строк, раскрытие по клику или `Tab`, обрезка до 2000 символов с эллипсисом. Файл транскрипта для этого не читаем: он пишется асинхронно и отстаёт от живого турна. Приёмка: в панели видно, что агент сказал последним, типовое закрывающее сообщение читается без раскрытия.

**S4 Полосы usage.** `AccountUsage` по секции 6.4, две `UsageBar` справа снизу под полем ввода: `5h` и `Week`, процент, обратный отсчёт до сброса, цвета по порогам. Каждый вариант `UsageUnavailable` рендерится прочерками со своей подписью. Приёмка: после тяжёлой сессии числа двигаются, панель при этом открывается мгновенно на прошлом снапшоте. Тесты: property на математику окон, путь ошибки на 128, 44, 401 и отсутствующий заголовок, контрактный тест на то, что медленный провайдер не задерживает `prompt-open`.

**S5 HUD задач.** `PostToolUse/TodoWrite` и события задач кормят `TaskItem`, HUD рендерит максимум 6 строк с пилюлями меток и точками статуса, `ScrollHint` появляется когда задач больше, колесо и трекпад скроллят при курсоре над HUD и никогда не забирают key-фокус. Пустое состояние прячет HUD целиком. Приёмка: HUD живьём отслеживает todo-список агента. Тесты: property на окно и на классификатор, контракт на фикстуре TodoWrite, отсутствие key-фокуса в ручном чек-листе.

**S6 Тумблер и island.** Регистрация `Alt+Shift+KeyQ` через плагин, переключение `enabled`, island показывает `Peekle is ON` или `Peekle is OFF` 1600 мс. OFF замыкает блокирующие эндпоинты на `200 {}`. Занятая комбинация: `hotkey_ok = false`, один варнинг, остальное работает. Приёмка: переключение посреди прогона меняет поведение агента на следующем хуке без перезапуска. Тесты: property на разбор комбинации, путь ошибки на провал регистрации, контрактный тест на замыкание эндпоинтов.

**S7 CLI-установщик.** `peekle init` пишет хуки и конфиг с бэкапом и идемпотентным мёржем, `peekle uninstall` снимает только свои записи, `peekle doctor` проверяет порт, токен, наличие хуков, запущенность приложения, доступ к Keychain, регистрацию комбинации и кап блокировок Stop из R-2, `peekle status` печатает состояние как JSON, `peekle off` переключает из шелла. Приёмка: на чистой машине `peekle init` плюс запуск дают рабочий оверлей, а второй `init` не меняет ничего. Тесты: мёрж `ClaudeSettings` поверх файла с чужими хуками, идемпотентность двойного `init`, `uninstall` не трогает чужое, провальные пути `doctor`.

**S8 Упаковка.** Ad-hoc подпись, dmg через `dmgbuild`, tap и cask Homebrew, npm-обёртка, которая тянет бинарь релиза в `postinstall` и кладёт бин `peekle`, `--version` совпадает с тегом, `tauri-plugin-updater` на проверку обновлений. Приёмка: `brew install nurikd/tap/peekle` даёт запускаемое приложение без предупреждения Gatekeeper, ручная установка dmg работает после right-click Open. Тесты: CI ставит свой артефакт на чистом раннере и гоняет `peekle doctor`.

**S9 Первый запуск.** Онбординг-окно: что делает Peekle, кнопка Connect Claude Code (тот же путь, что `peekle init`), кнопка Grant usage access с явным объяснением, зачем нужен доступ к Keychain, тумблер Open at Login через `tauri-plugin-autostart`, состояние подключения. Плюс понятные сообщения на все провальные пути `doctor`: занятый порт, отсутствующие хуки, занятая комбинация, отказ в Keychain. GUI настроек нет, конфиг это файл. Приёмка: у каждого провального режима из S7 есть видимый путь к починке.

## 15. Ручной чек-лист нативного поведения

Автотесты панель не покрывают. Прогонять перед каждым релизом:

- [ ] панель появляется поверх фуллскрин-видео в другом Space
- [ ] при появлении HUD в терминале не теряется ни одно нажатие
- [ ] панель запроса принимает ввод, но строка меню всё ещё показывает другое приложение
- [ ] панель не исчезает при деактивации приложения
- [ ] ответ прячет панель примерно за 120 мс
- [ ] нет иконки в доке, в трее, в строке меню, нет кнопки закрытия
- [ ] HUD читается и на светлых, и на тёмных кадрах видео
- [ ] ⌥⇧Q переключает состояние, текст в island совпадает с состоянием
- [ ] после Grant usage access повторный запуск приложения не поднимает диалог Keychain снова

## 16. Риски и открытые вопросы

- **R-1 занятая комбинация.** `Alt+Shift+KeyQ` может держать другое приложение, тогда регистрация вернёт ошибку. Обрабатываем как в 6.9: флаг, варнинг, `doctor`, всё остальное живёт. Смена комбинации через конфиг. Проблема двойного `q` (плеер закрывается, ложные срабатывания при наборе) закрыта переходом на комбинацию, к event tap не возвращаемся.
- **R-2 кап блокировок Stop.** Claude Code ограничивает число подряд идущих блокировок `Stop`-хука в сессии (`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`, дефолт 8). Peekle блокирует на каждый ответ, поэтому длинная сессия в него влетит. Проверить точное поведение и версию на стадии 1, дальше `doctor` предупреждает, а README говорит выставить 0.
- **R-3 заголовки usage недокументированы.** Числа снимаются с `anthropic-ratelimit-unified-*`. Формат может поменяться или пропасть. Провайдер за трейтом, `Unavailable(Unsupported)` всегда поддерживаемое состояние рендера, числа не додумываем. Слова limit и quota в UI не используем.
- **R-4 payload-ы событий задач.** Точные тела `TaskCreated`, `TaskCompleted` и `PostToolUse/TodoWrite` берутся из захвата, а не из документации. Захватить до S5 и поправить секцию 6 с бампом версии, если разошлось.
- **R-5 таймаут хука.** Таймаутнувший хук не даёт решения и агент останавливается штатно. `prompt_timeout_secs` держим ниже `timeout` хука, зазор виден в `doctor`.
- **R-6 доступ к Keychain.** Нужен только для usage, спрашивается по действию пользователя, отзывается пользователем. Все остальные фичи работают без него. Разрешение Accessibility не нужно нигде, это следствие перехода на комбинацию клавиш.
- **R-7 подписание.** Идём без платного аккаунта Apple: ad-hoc подпись, установка через Homebrew (каск снимает quarantine, предупреждения нет), ручная установка требует right-click Open один раз. Нотаризация это бэклог после первых пользователей, ничего не блокирует.
- **R-8 фиксированный порт.** URL хука в `settings.json` содержит порт, поэтому порт не может быть динамическим. Занят: `init --port` перезаписывает хуки, `doctor` говорит об этом прямо. План Б, если конфликты пойдут в жалобы: command-хук с генерируемым `bridge.sh`, читающим порт из port-файла и пробрасывающим ответ curl в stdout. Он же даст `$TERM_PROGRAM` для будущей фичи прыжка в терминал. В v1 не делаем.

## 17. Что берём из claudecodenotify

Репозиторий `narlei/claudecodenotify`, Swift, меню-бар приложение. Соседний продукт, а не конкурент: он уведомляет и отправляет в терминал, решение принимается там. Механики, проверенные им и переносимые к нам:

1. **Usage через аккаунт, а не через API-ключ.** OAuth-токен из Keychain плюс rate-limit заголовки ответа. Полностью в секции 6.4, включая пятиминутный кулдаун на refresh через CLI.
2. **Чтение Keychain через `/usr/bin/security`, а не через SecItem в своём процессе.** ACL привязывается к бинарю, `security` стабильно подписан Apple, поэтому Always Allow держится между обновлениями приложения. Для ad-hoc подписанного бинаря это разница между одним диалогом и диалогом на каждый запуск.
3. **Локальный сервер на 127.0.0.1 с токеном.** Токен в файле с правами 0600, наружу не уходит ничего.
4. **Установка хуков с бэкапом и дедупом.** Список управляемых событий одной константой, свои записи опознаются по признаку и заменяются, чужие не трогаются, `settings.json` бэкапится с таймстемпом, есть обратная операция uninstall.
5. **Распространение без нотаризации.** Ad-hoc подпись, dmg через `dmgbuild` без автоматизации Finder, tap Homebrew как основной путь (снимает предупреждение Gatekeeper), инструкция `xattr -dr com.apple.quarantine` для ручной установки.
6. **Пороги цветов на полосах usage и обратный отсчёт до сброса.** Зелёный, жёлтый, оранжевый, красный.
7. **Open at Login и проверка обновлений** как ожидаемая гигиена для такого приложения. У нас это плагины Tauri.

Что осознанно делаем иначе:
- Хуки блокирующие и через `type: "http"`, а не fire-and-forget через `bridge.sh`. Это ядро продукта: ответ пользователя возвращается агенту.
- Нет меню-бара и нет иконки статуса. Состояние показывает island, диагностику показывает `doctor`.
- Нет прыжка в терминал по Enter. Смысл Peekle в том, чтобы в терминал не идти. Прыжок это бэклог, и он потребует `$TERM_PROGRAM`, то есть плана Б из R-8.
